# Сборщик цен не публикуем: он документирует внутренние API четырёх площадок.
# Это отдельное решение, а не часть публикации калькулятора.
scraper/

node_modules/
*.log
.DS_Store
Thumbs.db

# локальный исходник; в репозиторий уходит его копия index.html
naryad-check.html
